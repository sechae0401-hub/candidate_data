# 취소사유분석기 FRD — AI 입력용 핸드오프 팩

> 본문: `frd-취소사유분석기-20260519.md`
> 사용처: trd-writer 입력, Claude Code 시스템 프롬프트, QA 테스트 케이스 작성
> 성격: 정밀 데이터 카탈로그. 처음부터 끝까지 읽는 용도가 아니다. 필요한 섹션을 부분 참조한다.

---

## 1. 화면 ID 카탈로그

| 화면 ID | 화면명 | URL | Tier | 권한 | 본문 §위치 |
|---|---|---|---|---|---|
| S-001 | 업로드 화면 | /upload | Core | 채매니저 (URL 직접 접근 허용) | §5.1 |
| S-002 | 분류 실행 중 | /analyzing | Core | 채매니저 (세션 없으면 /upload 리다이렉트) | §5.2 |
| S-003 | 분류 결과 | /result | Core | 채매니저 (세션 없으면 /upload 리다이렉트) | §5.3 |
| S-004 | 대시보드 | /dashboard | Tier 1 | 채매니저 (세션 없으면 /upload 리다이렉트) | §5.4 |

---

## 2. 권한 매트릭스 (정밀)

| 화면·기능 | 채매니저 (v1) | 직접 URL 접근 (세션 없음) | 토큰 열람자 (v2) |
|---|---|---|---|
| S-001 /upload | ✅ | ✅ | ❌ |
| S-002 /analyzing | ✅ | ❌ → /upload | ❌ |
| S-003 /result | ✅ | ❌ → /upload | ❌ |
| S-004 /dashboard | ✅ | ❌ → /upload | v2에서 읽기 전용 ✅ |
| 분류 실행 (GPT 호출) | ✅ | ❌ | ❌ |
| 인라인 수정 | ✅ | ❌ | ❌ |
| 노션 출력 | ✅ | ❌ | ❌ |
| 프리셋 저장/불러오기 | ✅ | ❌ | ❌ |

세션 감지 방식: localStorage의 `session_id` 키 존재 여부 확인. 없거나 해당 세션의 status가 'pending' 미만이면 /upload로 리다이렉트.

---

## 3. 요소 ID 카탈로그 (화면별)

### 3.1 S-001 업로드 화면

| 요소 ID | 요소명 | 타입 | 라벨·기본값 | 상태 조건 | 동작 |
|---|---|---|---|---|---|
| E-001 | 파일 업로드 영역 | Dropzone | "파일을 여기 끌어다 놓거나 클릭하세요" | 항상 표시 | 클릭/드롭 → 파일 파싱 시작 |
| E-002 | 파일 형식 안내 | Text | "xlsx, xls 파일만 지원 · 최대 10MB" | 항상 표시 | — |
| E-003 | 파일명 + 행 수 표시 | Text | — | 파싱 완료 후 표시 | — |
| E-004 | 기수명 입력 필드 | Input[text] | placeholder: "예: 29기" | 항상 표시 | 최대 100자 |
| E-005 | 자동 감지 버튼 | Button | "자동 감지 시도" | 파싱 완료 후 활성 | GPT-5.5 컬럼 감지 호출 |
| E-006 | 인터뷰 내용 드롭다운 | Select | "선택하세요" | 파싱 완료 후 표시. 필수(✱) | 컬럼 매핑 |
| E-007 | 특이사항 드롭다운 | Select | "선택하세요" | 파싱 완료 후 표시. 필수(✱) | 컬럼 매핑 |
| E-008 | 최종결과 드롭다운 | Select | "선택하세요" | 파싱 완료 후 표시. 필수(✱) | 매핑 완료 시 체크박스 표시 트리거 |
| E-009 | 유입경로 드롭다운 | Select | "선택하세요 (선택)" | 파싱 완료 후 표시 | 선택 안 해도 분류 가능 |
| E-010 | 최종결과 값 체크박스 그룹 | CheckboxGroup | — | E-008 선택 완료 후 표시 | 선택 건수 합계 실시간 갱신 |
| E-011 | 전체 선택 체크박스 | Checkbox | "전체 선택" | E-010과 함께 표시 | 전체 선택/해제 |
| E-012 | 분석 대상 건수 표시 | Text | "선택된 분석 대상: N건 / 전체 M행" | 체크박스 1개 이상 선택 시 표시 | — |
| E-013 | 프리셋 불러오기 버튼 | Button | "저장된 프리셋 불러오기" | 프리셋 존재 시 활성 | 드롭다운 + 체크박스 상태 복원 |
| E-014 | 프리셋 저장 버튼 | Button | "현재 설정을 프리셋으로 저장" | 항상 표시 | 동일 이름 존재 시 덮어쓰기 확인 팝업 |
| E-015 | 분류 실행 버튼 | Button | "분류 실행" | 필수 3개 + 체크박스 1개 이상 → 파란색 활성. 그 외 회색 비활성 | S-002 이동 + session 저장 |
| E-016 | 중단 세션 감지 팝업 | Modal | "이전 분류가 중단되었습니다" | 재접속 시 localStorage session_id + status='analyzing' 감지 시 표시 | "새로 시작" → 이전 세션 failed 처리 |

### 3.2 S-002 분류 실행 중 화면

| 요소 ID | 요소명 | 타입 | 라벨·기본값 | 상태 조건 | 동작 |
|---|---|---|---|---|---|
| E-020 | 진행 카운터 | Text | "N건 분류 중 · M / N 완료" | 1단계 분류 중 표시 | 3건 완료마다 갱신 |
| E-021 | 진행 바 | ProgressBar | 0% → 100% | 1단계 분류 중 표시 | M/N 비율로 갱신 |
| E-022 | 인사이트 생성 안내 | Text | "분류 완료! 인사이트 요약 생성 중..." | 2단계(인사이트 생성) 시 표시 | — |
| E-023 | 부분 실패 배너 | Banner(yellow) | "N건 분류 실패 — 검토 필요로 자동 처리됩니다" | 부분 실패 시 표시 | 분류 중단 없이 계속 |
| E-024 | 전체 실패 메시지 | Text | "전체 분류에 실패했습니다" | 전체 실패 시 표시 | — |
| E-025 | 재시도 버튼 | Button | "재시도" | 전체 실패 시 표시 | 처음부터 재분류 |
| E-026 | 업로드 화면 복귀 버튼 | Button | "업로드 화면으로 돌아가기" | 전체 실패 시 함께 표시 | 현재 세션 failed → /upload |
| E-027 | 다중탭 방지 안내 | Toast | "이미 분류가 진행 중입니다" | 다른 탭에서 실행 시도 시 | localStorage 'classifying=true' 감지 |

### 3.3 S-003 분류 결과 화면

| 요소 ID | 요소명 | 타입 | 라벨·기본값 | 상태 조건 | 동작 |
|---|---|---|---|---|---|
| E-030 | 상단 요약 바 | StatusBar | "전체 N건 \| 검토 필요 M건 · 완료 K건 \| 신규 카테고리 J개" | 항상 표시. 실시간 갱신 | — |
| E-031 | 분류 결과 테이블 | Table | 전체 분류 결과 | 항상 표시 | 행 클릭 → E-032 사이드패널 활성 |
| E-032 | 원문 사이드 패널 | Panel | 인터뷰 내용 + 특이사항 원문 | 행 클릭 시 화면 우측 40% | 행 변경 시 내용 교체 |
| E-033 | 1차원인 편집 셀 | EditableCell[text] | GPT 분류값 | 검토필요 행: 노란 배경. 완료 행: 초록 배경 | blur → 자동 저장. 빈값 허용 (단 review_completed 차단) |
| E-034 | 2차행동 편집 셀 | EditableCell[text] | GPT 분류값 | 동일 | blur → 자동 저장 |
| E-035 | 세부태그 편집 셀 | EditableCell[text] | 쉼표 구분 텍스트 | 동일 | blur → 쉼표로 분리 → text[] 변환 후 저장. 태그당 최대 50자 |
| E-036 | 검토필요여부 토글 | Toggle | "예" / "아니오" | 검토필요 행에서 활성 | "아니오" 선택 + 1차원인 있음 → review_completed=true → 초록 전환 |
| E-037 | 노션 출력 버튼 | Button | "노션 형식으로 복사" | 모든 needs_review 행 완료 시 파란색. 그 외 회색 | 클립보드 복사. 실패 시 E-038 폴백 |
| E-038 | 노션 폴백 텍스트박스 | Textarea | 마크다운 테이블 전체 | 클립보드 복사 실패 시 표시 | 채매니저가 직접 복사 |
| E-039 | 신규 카테고리 섹션 | Section | 신규 카테고리 목록 | 1개 이상 시 표시. 0개면 미표시. 최대 10개 + "N개 더 보기" | — |
| E-040 | 대시보드 이동 버튼 | Button | "대시보드 보기" | 항상 활성 | S-004 이동. 검토 미완료 시 배너 표시 |
| E-041 | 저장됨 토스트 | Toast | "저장됨 ✓" | 셀 저장 성공 시 1초 | — |
| E-042 | 복사됨 토스트 | Toast | "복사됨!" | 클립보드 복사 성공 시 1초 | — |

### 3.4 S-004 대시보드 화면

| 요소 ID | 요소명 | 타입 | 라벨·기본값 | 상태 조건 | 동작 |
|---|---|---|---|---|---|
| E-050 | 검토 미완료 배너 | Banner(yellow) | "아직 N건 검토가 남았습니다 → S-003으로 돌아가기" | S-003 검토 미완료 상태로 진입 시 | 링크 클릭 → S-003 |
| E-051 | 기수명 + 분석일시 | Header | "[기수명] · [분석일시] · 전체 N건" | 항상 표시 | — |
| E-052 | GPT 인사이트 카드 | Card | 한 단락 요약 + "⚠️ AI 자동 생성 요약입니다. 참고용으로만 활용하세요" | 생성 성공 시 표시. 실패 시 미표시 | — |
| E-053 | 탭 네비게이션 | Tabs | 전체 / 유입경로별 / 진행단계별 | 항상 표시 | 탭 전환 시 추가 API 호출 없음 |
| E-054 | 취소사유 TOP5 차트 | HorizontalBar | — | 전체 탭 | 건수 1~4건 시 제목 "취소 사유 분포"로 변경 |
| E-055 | 후속행동 도넛 차트 | Donut | — | 전체 탭 | — |
| E-056 | 타과정 언급 표 | Table | — | 전체 탭. 언급 없으면 미표시 | — |
| E-057 | 검토필요 목록 | Table(readonly) | — | 전체 탭. 0건 시 "검토 필요 항목 없음 ✅" | — |
| E-058 | 유입경로별 건수 차트 | VerticalBar | — | 유입경로 탭. 미매핑 시 안내 문구 + /upload 링크 | — |
| E-059 | 유입경로별 TOP3 원인 차트 | GroupedHorizontalBar | — | 유입경로 탭 | — |
| E-060 | 진행단계별 건수 차트 | VerticalBar | — | 진행단계 탭 | — |
| E-061 | 진행단계별 TOP3 원인 차트 | GroupedHorizontalBar | — | 진행단계 탭 | — |
| E-062 | 0건 Empty 안내 | Text | "분류된 결과가 없습니다. S-003으로 돌아가 확인해 주세요" | 분류 결과 0건 시 | 링크 클릭 → S-003 |

---

## 4. 검증 규칙 (필드별)

| 화면·요소 | 필드 | 클라이언트 검증 | 서버 검증 | 에러 메시지 |
|---|---|---|---|---|
| S-001 / E-001 | 파일 형식 | 확장자 .xlsx/.xls 확인 | — | "xlsx 또는 xls 파일만 지원합니다" |
| S-001 / E-001 | 파일 크기 | 10MB 이하 (클라이언트 즉시 차단) | — | "10MB 이하 파일만 지원합니다 (현재: N.NMB)" |
| S-001 / E-004 | 기수명 | 최대 100자 초과 입력 불가 | — | 입력 자체 차단 (100자 이후 입력 무시) |
| S-001 / E-035 | 세부태그 각 태그 | 각 태그 최대 50자 | — | 50자 초과 태그는 저장 시 잘라냄 + 경고 토스트 |
| S-003 / E-033 | 1차원인 빈값 | 빈값 저장 허용. 단 review_completed=true 차단 | — | 검토필요여부 토글 "아니오" 불가 안내 |
| S-003 / E-035 | 세부태그 쉼표 분리 | 쉼표 기준 분리. 공백 trim | — | — |
| S-001 / E-010 | 체크박스 선택 | 1개 이상 필수 | — | "분석할 취소 유형을 1개 이상 선택해 주세요" (버튼 하단) |

---

## 5. 인수 기준 (Acceptance Criteria)

### 5.1 S-001 업로드 화면

- [ ] xlsx/xls 외 파일 드롭 시 즉시(0ms) 빨간 에러 메시지 표시
- [ ] 파일 크기 10MB 초과 시 클라이언트에서 즉시 차단 (서버 전송 없음)
- [ ] 파일 파싱 완료 후 2초 이내 컬럼 드롭다운 표시
- [ ] 최종결과 드롭다운 선택 완료 후 0.5초 이내 체크박스 목록 표시
- [ ] 필수 3개 컬럼 미완료 시 분류 실행 버튼 비활성(회색) 유지
- [ ] 체크박스 1개 이상 선택 후 분류 실행 버튼 파란색 활성화
- [ ] 프리셋 저장 클릭 후 1초 이내 저장 완료 피드백
- [ ] 프리셋 불일치 컬럼 드롭다운에 빨간 테두리 표시
- [ ] 기수명 100자 초과 입력 불가 (101번째 문자부터 무시)
- [ ] 재접속 시 localStorage session_id 존재 + status='analyzing' 감지 → 팝업 표시

### 5.2 S-002 분류 실행 중 화면

- [ ] 진행 카운터 3건 완료마다 갱신 (최대 3초 지연)
- [ ] 전체 분류 완료 후 2초 이내 S-003 자동 이동
- [ ] 부분 실패 시 노란 배너 표시, 분류 중단 없이 계속
- [ ] 전체 실패 시 재시도 버튼 + 업로드 복귀 버튼 표시
- [ ] 이탈 시도 시 브라우저 기본 경고 표시 (beforeunload)
- [ ] 인터뷰·특이사항 둘 다 빈 행은 GPT 전송 제외 처리
- [ ] localStorage 'classifying=true' 중 다른 탭에서 실행 시 토스트 안내

### 5.3 S-003 분류 결과 화면

- [ ] 검토 필요 행 노란 배경 표시
- [ ] 행 클릭 후 300ms 이내 사이드 패널 원문 표시
- [ ] 편집 셀 blur 후 1초 이내 자동 저장 + 저장됨 토스트
- [ ] 저장 실패 시 해당 셀 빨간 테두리 + 재시도 안내
- [ ] 1차원인 빈값 상태에서 검토필요여부 "아니오" 불가 처리
- [ ] 모든 needs_review 행 review_completed=true 시 노션 버튼 파란색 활성화
- [ ] 노션 복사 성공 시 "복사됨!" 토스트 1초 표시
- [ ] 노션 복사 실패 시 텍스트박스 폴백 표시
- [ ] 신규 카테고리 10개 초과 시 "N개 더 보기" 접기 처리

### 5.4 S-004 대시보드 화면

- [ ] 탭 전환 즉시 (300ms 이내, 추가 API 호출 없음)
- [ ] 차트 개별 스켈레톤 UI → 순차적 렌더링 (전체 로딩 2초 이내)
- [ ] 검토 미완료 상태 진입 시 노란 배너 + S-003 링크 표시
- [ ] 유입경로 미매핑 시 해당 탭에 안내 문구 + /upload 링크 표시
- [ ] 분류 결과 0건 시 "S-003으로 돌아가 확인해 주세요" 안내 표시
- [ ] 진행단계별 파싱 — 신청취소(본인요청): 인터뷰 내용 유무로 전/후 자동 분류

---

## 6. 인터랙션 명세 (이벤트 → 결과)

| 화면 | 이벤트 | 대상 요소 | 동작 | 디바운스·딜레이 |
|---|---|---|---|---|
| S-001 | change | E-008 최종결과 드롭다운 | 해당 컬럼 고유 값 조회 → E-010 체크박스 생성 | 0ms |
| S-001 | change | E-010 체크박스 | E-012 선택 건수 합계 갱신. 분류 실행 버튼 상태 재평가 | 0ms |
| S-001 | click | E-005 자동 감지 버튼 | POST /api/detect-columns. 결과로 드롭다운 채움 | — |
| S-001 | click | E-015 분류 실행 버튼 | POST /api/sessions. localStorage session_id 저장. localStorage classifying=true. S-002 이동 | — |
| S-003 | click | 테이블 행 | E-032 사이드 패널 원문 표시 | 300ms 이내 |
| S-003 | blur | E-033 1차원인 셀 | PATCH /api/results/:id. 저장됨 토스트 | 0ms (즉시) |
| S-003 | blur | E-034 2차행동 셀 | 동일 | 0ms |
| S-003 | blur | E-035 세부태그 셀 | 쉼표 분리 → text[] 변환 → PATCH /api/results/:id | 0ms |
| S-003 | change | E-036 검토필요여부 토글 → "아니오" | 1차원인 빈값 시 차단. 그 외: review_completed=true → 초록 전환. 요약 바 갱신 | 0ms |
| S-003 | click | E-037 노션 출력 버튼 | navigator.clipboard.writeText(markdown). 성공: E-042 토스트. 실패: E-038 텍스트박스 표시 | — |
| S-004 | click | E-053 탭 | 해당 탭 차트·표 렌더링 (캐시된 데이터, 추가 API 없음) | 0ms |

---

## 7. 외부 의존성 상세

| 서비스 | 용도 | 엔드포인트 (Next.js API Route) | 인증 | 비용 모델 | 실패 시 영향 |
|---|---|---|---|---|---|
| OpenAI GPT-5.5 | 취소 사유 분류 | POST /api/classify (3건씩 순차 호출) | OPENAI_API_KEY (Vercel 환경변수) | 기수당 $0.50 이하 목표 | S-002 전체 실패 또는 부분 실패 처리 |
| OpenAI GPT-5.5 | 컬럼 자동 감지 | POST /api/detect-columns | 동일 | 기수당 1회 | E-005 버튼 실패 안내 → 수동 지정 |
| OpenAI GPT-5.5 | 인사이트 요약 | POST /api/insight (분류 완료 후 1회) | 동일 | 기수당 1회 | 조용히 건너뜀. S-004 인사이트 섹션 미표시 |
| Supabase | 세션 저장 | Supabase JS Client (서버 사이드) | SUPABASE_URL, SUPABASE_ANON_KEY | 무료 플랜 (500MB, 월 50만 API 호출) | 결과 저장 불가. 새로고침 시 소실 |
| Supabase | 분류 결과 조회·수정 | 동일 | 동일 | 동일 | S-003 로딩 실패 |
| SheetJS (xlsx) | 엑셀 파싱 | 클라이언트 사이드 (npm 패키지) | 없음 | 무료 | S-001 파싱 실패 오류 |
| Recharts | 차트 렌더링 | 클라이언트 사이드 (npm 패키지) | 없음 | 무료 | 차트 미표시. 표만 표시 |

**GPT-5.5 호출 구조**

```
클라이언트 → POST /api/classify (3건씩 순차)
  Body: { rows: [...3건...], session_id: "uuid" }
  Response: JSON 배열 (8개 필드)
  실패 시 필드 기본값: { primary_cause: "", secondary_action: "",
    detail_tags: [], competing_course: null, reasoning: "파싱 오류",
    needs_review: true, new_category: null }
  Vercel 함수 실행 제한: 10초 이내 (3건 기준 충분)
```

**Supabase 테이블 (PRD 핸드오프 §3 스키마 그대로 사용)**

- `sessions`: id, cohort_name, total_rows, excluded_rows, status, insight_summary, analyzed_at, created_at
- `classification_results`: id, session_id, row_index, interview_content, special_notes, primary_cause, secondary_action, detail_tags, competing_course, reasoning, needs_review, new_category, review_completed, created_at
- `column_mappings`: id, preset_name, mapping_config ({"interview": "컬럼명", "notes": "컬럼명", "result": "컬럼명", "source": "컬럼명 또는 null"}), is_active, created_at
- `new_categories`: id, session_id, category_name, occurrence_count, created_at

**column_mappings.mapping_config 변경 사항**: PRD 핸드오프의 `"status"` 키를 `"result"`로 변경. `"stage"` 키 제거 (진행 단계는 최종결과 값에서 자동 파싱). 체크박스 선택 값은 별도 `selected_result_values text[]` 컬럼을 sessions 테이블에 추가하여 저장.

---

## 8. 5개 상태 상세 (화면별)

### S-001

| 상태 | 트리거 | 보이는 것 | 가능한 액션 | 시스템 처리 |
|---|---|---|---|---|
| Empty | 첫 접속 | 업로드 영역 강조. 컬럼 섹션·체크박스 미표시 | 파일 업로드 | localStorage session_id 확인 → analyzing 세션이면 팝업 |
| Loading (파싱) | 파일 드롭/클릭 | 업로드 영역 스피너 "파일을 읽는 중..." | 없음 (대기) | SheetJS 파싱. 완료 시 컬럼 헤더 추출 |
| Loading (감지) | E-005 클릭 | 버튼 스피너 "컬럼 감지 중..." | 없음 (대기) | POST /api/detect-columns |
| Error (파일) | 파싱 실패 / 형식 오류 | 업로드 영역 빨간 텍스트 | 다른 파일 업로드 | — |
| Error (감지) | 자동 감지 실패 | 컬럼 섹션 상단 노란 안내 | 수동 지정 | 드롭다운 편집 상태 유지 |
| Disabled (매핑) | 파싱 성공, 필수 컬럼 미완료 | 분류 버튼 회색. 미완료 컬럼 구체적 안내 | 드롭다운 선택 | — |
| Disabled (체크박스) | 필수 3개 완료, 체크박스 0개 | 분류 버튼 회색. "취소 유형 1개 이상 선택" 안내 | 체크박스 선택 | — |
| Success | 필수 3개 + 체크박스 1개 이상 | 분류 버튼 파란색. 선택 건수 표시 | 분류 실행 | — |

### S-002

| 상태 | 트리거 | 보이는 것 | 가능한 액션 | 시스템 처리 |
|---|---|---|---|---|
| Loading (분류) | S-001 실행 버튼 클릭 | 카운터 + 진행 바. 부분 실패 시 노란 배너 | 없음 (대기) | POST /api/classify 3건씩 순차 |
| Loading (인사이트) | 전체 분류 완료 | "인사이트 요약 생성 중..." 스피너 | 없음 (대기) | POST /api/insight |
| Error (부분) | 일부 건 API 오류 | 노란 배너 "N건 실패 — 검토 필요 처리" | 없음 (계속 진행) | 실패 행 needs_review=true 자동 태깅 |
| Error (전체) | 치명적 오류 | "전체 실패" 메시지 + 재시도/복귀 버튼 | 재시도 또는 복귀 | 세션 status='failed' |
| Disabled (이탈) | 이탈 시도 | 브라우저 기본 경고 팝업 | 머무르거나 이탈 | beforeunload 이벤트 |

### S-003

| 상태 | 트리거 | 보이는 것 | 가능한 액션 | 시스템 처리 |
|---|---|---|---|---|
| Loading | S-002 완료 후 진입 | 테이블 스켈레톤 UI | 없음 (대기) | GET /api/results/:session_id |
| Error (로딩) | Supabase 조회 실패 | "불러오지 못했습니다" + 새로고침 | 새로고침 | — |
| Success (검토 미완료) | 정상 로딩 | 노란 행 하이라이트. 노션 버튼 회색 | 셀 편집, 행 클릭, 대시보드 이동 | — |
| Success (검토 완료) | 모든 needs_review 행 완료 | 초록 행. 노션 버튼 파란색 | 노션 출력, 대시보드 이동 | — |
| Error (저장) | PATCH 실패 | 셀 빨간 테두리 + 재시도 안내 | 재시도 | — |

### S-004

| 상태 | 트리거 | 보이는 것 | 가능한 액션 | 시스템 처리 |
|---|---|---|---|---|
| Loading | 진입 | 차트 영역 스켈레톤 UI (순차 렌더링) | 없음 (대기) | Supabase 집계 쿼리 |
| Error (로딩) | Supabase 조회 실패 | 섹션별 "불러오지 못했습니다" + 새로고침 | 섹션별 새로고침 | — |
| Success | 정상 로딩 | 차트 + 표. 검토 미완료 배너 (해당 시) | 탭 전환, S-003 복귀 | — |
| Empty (0건) | 분류 결과 0건 | "분류된 결과가 없습니다" 안내 + 링크 | S-003 이동 | — |
| Disabled (유입경로) | 유입경로 미매핑 | 유입경로 탭에 안내 문구 + /upload 링크 | /upload 이동 | — |

---

## 9. Blindspot Check 상세 결과

### 9.1 화면 상태 정의

- S-002 Success 상태 없음 → 완료 즉시 S-003 자동 이동으로 의도적 처리. 허용.
- S-004 Empty 상태 추가 → 분류 결과 0건 시 안내 문구 + 링크.

### 9.2 예외 흐름

- **강제이탈 재접속**: localStorage session_id + Supabase status 조회로 감지. 팝업 → 이전 세션 failed 처리.
- **부분 실패**: 실패 행 needs_review=true 자동 태깅. 분류 계속 진행. 노란 배너 표시.
- **인터뷰·특이사항 둘 다 빈 행**: GPT 전송 제외. needs_review=true + reasoning="인터뷰 내용 및 특이사항 없음".
- **GPT JSON 필드 누락**: 필드별 기본값 적용 (§7 참조).
- **노션 복사 실패**: 텍스트박스 폴백 (E-038).
- **다중 탭 동시 실행**: localStorage 'classifying=true' 플래그로 방지.

### 9.3 권한

- 세션 없이 S-002~S-004 접근 시 /upload 리다이렉트.
- GPT-5.5 API 키 브라우저 미노출 (서버 사이드 전용 호출).
- v2 토큰 링크는 별도 PRD 및 dashboard_tokens 테이블 활성화 필요.

### 9.4 검증 규칙

- 파일 형식: 클라이언트 확장자 체크.
- 파일 크기: 클라이언트 10MB 즉시 차단.
- 기수명: 100자 초과 입력 불가.
- 세부태그: 태그당 50자 초과 시 잘라냄 + 경고 토스트.
- 1차원인 빈값: review_completed 차단으로 출력 품질 보호.

### 9.5 엣지 케이스

- 취소 건 1~4건: 차트 제목 "취소 사유 분포"로 변경.
- 신규 카테고리 10개 초과: 접기 처리 + "N개 더 보기".
- 프리셋 이름 중복: 덮어쓰기 확인 팝업.
- 신청취소(본인요청) 진행단계 분류: 인터뷰 내용 유무로 자동 판단.

---

## 10. 미해결 / TRD에서 결정할 항목

| 항목 | 이유 | TRD 처리 방향 |
|---|---|---|
| Next.js App Router vs Pages Router | FRD 범위 밖. 구현 방식 선택 | TRD §기술스택에서 확정 |
| Supabase 서버 사이드 클라이언트 vs 클라이언트 사이드 | RLS 정책과 키 노출 관련 | TRD §API 설계에서 확정 |
| Vercel 서버리스 함수 vs Edge Runtime | 10초 제한 대응 방식 | TRD §인프라에서 확정 |
| GPT-5.5 Rate Limit 대응 | 500 RPM 초과 시 처리 | TRD §외부 의존성에서 확정 |
| sessions 테이블에 selected_result_values 컬럼 추가 | PRD 스키마에 없음. FRD에서 필요 확인 | TRD §DB 스키마에서 공식 추가 |
| 토큰 링크 공유 (v2) | 별도 PRD 필요 | v2 설계 시 처리 |
| 기수 간 비교 대시보드 (v2) | 데이터 저장 구조 확장 필요 | v2 설계 시 처리 |

---

## 11. TRD 작성 시 사용할 핵심 변수

| 변수 | 값 |
|---|---|
| 화면 총수 | 4개 (S-001~S-004) |
| Core 화면 | S-001, S-002, S-003 |
| Tier 1 화면 | S-004 |
| v1 출시 총 기능 | 30개 (필수 28 + 선택 2: F-006, F-034) |
| v1.x 후순위 | F-009, F-016, F-025, F-040 |
| v2 이후 | F-037, F-038, F-039 (토큰 링크) |
| AI 엔진 | OpenAI GPT-5.5 (gpt-5.5-2026-04-23) |
| API 호출 구조 | POST /api/classify — 3건씩 클라이언트 순차 요청 |
| DB | Supabase 무료 플랜. 테이블 4개 + sessions에 selected_result_values 추가 필요 |
| 호스팅 | Vercel 무료 플랜. 함수 실행 10초 제한 |
| NFR 핵심 수치 | 50건 5분 이내, 함수 10초 이내, 파일 10MB/200행 이하 |
| 필수 컬럼 (매핑) | 인터뷰 내용, 특이사항, 최종결과 (3개 필수) + 유입경로 (1개 선택) |
| 진행단계 파싱 | 최종결과 값 하드코딩 규칙 (핸드오프 §8 S-004 참조) |
| 노션 TOP3 생성 | primary_cause 집계 상위 3개. GPT 추가 호출 없음 |
| 세션 감지 | localStorage session_id + Supabase status 조회 |
| 다중탭 방지 | localStorage 'classifying' 플래그 |

---

## 12. 다음 스킬 호출

**다음 단계**: trd-writer
**호출 방법**: `"TRD 작성해줘"`
**trd-writer에 제공할 입력 (총 4개 파일)**:
1. `prd-취소사유분석기-20260519.md` (PRD 본문)
2. `prd-취소사유분석기-20260519-handoff.md` (PRD 핸드오프)
3. `frd-취소사유분석기-20260519.md` (FRD 본문 — 본 파일 파트너)
4. `frd-취소사유분석기-20260519-handoff.md` (본 파일)

**TRD에서 가장 먼저 다룰 항목**:
- Next.js App Router 기반 서버 액션 vs API Route 선택
- Supabase sessions 테이블에 `selected_result_values text[]` 컬럼 공식 추가
- GPT-5.5 Rate Limit(500 RPM) 초과 대응 전략
- Vercel 서버리스 10초 제한 — 3건 묶음 호출 구현 방식 확정
